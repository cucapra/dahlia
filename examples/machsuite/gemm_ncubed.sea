func gemm( m1: TYPE[N][N bank(N)], m2: TYPE[N bank(N)][N], prod: TYPE[N][N], mult: TYPE, sum: TYPE ){

    outer: for (let i = 0..row_size-1) unroll 1 {
        middle: for (let j = 0..col_size-1) unroll 1 {
            sum = (TYPE)0;
            inner: for( let k = 0..row_size-1) unroll N {
                mult = m1[i][k] * m2[k][j];
                sum += mult;
            }
            prod[i][j]  = sum;
        }
    }

}
