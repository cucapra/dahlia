(menhir
  (modules parser))

(ocamllex lexer)

(library
  (name seashell)
  (preprocess (pps ppx_deriving.std))
  (libraries core ppx_deriving.runtime cmdliner extlib menhir))

(env
  (default
    (flags (:standard -short-paths -w +a-4 -warn-error +a-4))))
