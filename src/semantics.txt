
Main types:
  - Expressions
  - Values
  - Commands

Expressions:
  - Boolean comparison operators:

Commands:
  - Assignment of a variable to a value

    <let x = e1, env> ==> env[x->v1]
      if <e1, env> ==> v1

  - For loop over a range

    <for (i in range x1..x2) { e1 }>

    FIXME: Cobbles old variables with same name i?
    1.) Create a new Assignment with i bound to x1, add to environment
    2.) Evaluate e1 with this new environment
    3.) Bind i to x1+1, evaluate e1 with this binding; repeat with
        x1+k until x1+k=x2
    4.) Remove binding

Values:
  - Booleans
  - Integers
