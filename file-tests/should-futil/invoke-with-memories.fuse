def add_one(a: ubit<32>[1][1]): ubit<32> = {
   let z: ubit<1> = (0 as ubit<1>);
   let _temp = (1 as ubit<32>) + a[z][z];
   return _temp;
}

decl X: ubit<32>[1][1];

let ret: ubit<32> = add_one(X);
