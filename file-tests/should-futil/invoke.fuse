import futil("primitives/math.futil") { def std_sqrt(in: ubit<32>): ubit<32>; }

def foo(a: ubit<32>): ubit<32> = {
  let temp: ubit<32> = a;
  return temp;
}

let b: ubit<32> = (1 as ubit<32>);
let c: ubit<32> = foo(b);
let d: ubit<32> = std_sqrt(c);
