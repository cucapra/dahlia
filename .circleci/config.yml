version: 2
jobs:
  build:

    docker:
      - image: hseeberger/scala-sbt

    steps:
      - checkout

      - run:
          name: Compile and Test
          command: |
            sbt ";compile; test"
