version: 2
jobs:
  build:

    docker:
      - image: ocaml/opam2

    steps:
      - checkout

      - run:
          name: Install OCaml packages
          command: |
            sudo apt-get install -y m4
            opam install .

      - run:
          name: Build Seashell and run examples
          command: |
            eval $(opam env)
            dune build
            dune install
            cd examples
            make all
